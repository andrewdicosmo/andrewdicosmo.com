{
  "name": "mission-file-portfolio",
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "prepare:content": "node scripts/use-example-content.mjs && node scripts/build-chat-knowledge.mjs",
    "predev": "npm run prepare:content",
    "prebuild": "npm run prepare:content",
    "dev": "astro dev",
    "build": "astro build && node -e \"require('node:fs').copyFileSync('staticwebapp.config.json','dist/staticwebapp.config.json')\"",
    "preview": "astro preview"
  },
  "engines": { "node": "24.x" },
  "dependencies": { "astro": "^4.16.0" }
}
